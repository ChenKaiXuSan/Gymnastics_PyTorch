# @package _global_
# Ablation: equal_reliability.
defaults:
  - override /model: archive/v1_1
  - override /loss: archive/v3
run_name: ${data.name}_${model.name}_equal_reliability_seed${seed}
model:
  reliability:
    enabled: false

# @package _global_
# Contrastive (InfoNCE) periodicity; now the default, preset kept for older commands.
run_name: ${data.name}_${model.name}_period_contrastive_seed${seed}
loss:
  periodicity:
    type: contrastive

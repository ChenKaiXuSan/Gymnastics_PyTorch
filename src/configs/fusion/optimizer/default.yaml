learning_rate: 3.0e-4
weight_decay: 1.0e-4
# Linear warm-up steps before the cosine decay.
warmup_steps: 100
# Final learning rate as a fraction of the base rate.
min_learning_rate_ratio: 0.05
betas: [0.9, 0.98]
